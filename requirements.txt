# Vehicle Speed Detection System — HuggingFace Spaces
# No supervision required — uses ultralytics built-in ByteTrack

ultralytics>=8.2.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
streamlit>=1.32.0
lapx>=0.5.2          # drop-in lap replacement — required by ultralytics ByteTrack